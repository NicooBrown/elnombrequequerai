import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.lithost.ALENIC',
  appName: 'Asis. Duoc',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
